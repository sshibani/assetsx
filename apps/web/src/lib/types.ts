export type {
  AccountDTO,
  AccountMembershipDTO,
  AuthAccountContext,
  AssetDTO,
  AuthTokens,
  Permission,
  PublicationDTO,
  RenditionDTO,
  UserDTO,
} from "@assetx/shared-types";

export interface ChannelInfoLike {
  id: string;
  label: string;
}
